# android/

Kotlin app (Jetpack Compose, Retrofit + OkHttp, Hilt, Room). Assumes Phase 1 MVP scaffold exists.
First changes land in F2.1 (doc_type chip on the document list). Do not scaffold ahead of the build order.
